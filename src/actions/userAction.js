const LOGIN_USER = 'LOGIN_USER'

const actionGetUser = (user) => {
    return {
        type: LOGIN_USER,
        user
    }
}

const getUser = ()  => disptach => {
    return
}

export {
    LOGIN_USER,

}